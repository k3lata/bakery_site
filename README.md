# bakery_site
# SweetLab  
Итоговый проект по HTML / CSS / JavaScript

##Тема проекта
Мини-магазин (сайт кафе / кондитерской) с онлайн-заказом.

Проект демонстрирует работу с:
- HTML,
- CSS (адаптивная верстка, Flexbox / Grid),
- JavaScript (DOM, события, логика, localStorage).


##  Описание проекта
**SweetLab** — это многостраничный мини-сайт кондитерской с каталогом товаров и корзиной.

Пользователь может:
- просматривать каталог десертов,
- добавлять товары в корзину,
- изменять количество товаров,
- удалять товары,
- видеть автоматический пересчёт суммы заказа,
- оформлять заказ (без backend, с очисткой корзины).

Проект выполнен в едином стиле и адаптирован под разные размеры экранов.


##Страницы сайта
- **Главная** (`index.html`)
- **Каталог товаров** (`catalog.html`)
- **Корзина** (`cart.html`)
- **О нас** (`about.html`)
- **Контакты** (`contacts.html`)

Все страницы связаны общей навигацией и имеют единый дизайн.


## 🎨 Используемые технологии

### HTML
- семантическая структура (`header`, `nav`, `main`, `section`, `article`, `footer`);
- корректная иерархия заголовков;
- отсутствие inline-стилей.

### CSS
Структура CSS разделена на 4 файла:
- `reset.css` — сброс стандартных стилей браузера;
- `base.css` — базовые стили, шрифты, цвета;
- `layout.css` — сетки, контейнеры, адаптивная верстка;
- `components.css` — кнопки, карточки товаров, корзина, UI-элементы.

Используются Flexbox и Grid.  
Верстка адаптивна для мобильных устройств, планшетов и десктопа.

---

## ⚙️ JavaScript-функционал
В проекте реализованы:
- работа с переменными разных типов;
- условия (`if / else`);
- циклы (`forEach`);
- пользовательские функции (добавление товара, изменение количества, расчет суммы);
- обработчики событий (`click`);
- работа с DOM (создание, удаление элементов, изменение текста);
- хранение данных корзины в `localStorage`.

### Основные возможности:
- добавление товаров в корзину;
- увеличение / уменьшение количества товаров;
- удаление товаров;
- автоматический пересчёт итоговой суммы;
- очистка корзины после оформления заказа.

## Форма и валидация
На странице **Контакты** реализована форма с:
- несколькими полями ввода;
- проверкой корректности email;

## Структура проекта
projectWeb/
├ index.html
├ pages/
│ ├ about.html
│ ├ catalog.html
│ ├ cart.html
│ ├ contacts.html
├ css/
│ ├ reset.css
│ ├ base.css
│ ├ layout.css
│ ├ components.css
├ js/
│ ├ catalog.js
│ ├ cart.js
│ ├ contacts.js
├ images/
│ └ (изображения товаров и фона)

